# 期货库存数据处理工具 - 使用说明 v5.0

> **当前版本**: v5.0 (TDD 重构版) | **更新日期**: 2026-05-10

---

## 📋 功能概述

本工具自动化处理期货库存数据，每次运行执行以下流程：

| 步骤 | 功能 | 说明 |
|------|------|------|
| 0 | **备份原文件** | 程序启动前自动备份 `期货库存明细.xlsx` → `results/期货库存明细_备份.xlsx`（覆盖模式） |
| 1 | 读取表头模板 | 优先使用客户 Sheet，提取标准 23 列表头 |
| 2 | 读取期货排程 | 提取订单号、客户名称、合同日期 |
| 3 | 读取烨辉库存表 | 建立钢卷号 → 仓别/移拨日期/入库日期 映射 |
| 3.5 | 读取原文件数据 | 加载所有客户 Sheet 中的现有钢卷数据 |
| 3.6 | 构建索引 | 以钢卷号为 key 建立快速查找映射 |
| 4 | 差异比对 | 以期货排程订单为基准，识别新增/更新/保留 |
| 5 | 重建 Sheet | 按客户分组，写入所有数据 |
| 6 | 保存文件 | 直接覆盖更新 `results/期货库存明细.xlsx` |

---

## 🔑 核心逻辑

### 处理规则（以钢卷号为唯一标识）

```
每次运行时：
┌─ 原文件已有的钢卷
│   ├─ 烨辉表中存在 + 仓别【有变动】  → 更新仓别/移拨日期/入库日期 🟠 橙色
│   ├─ 烨辉表中存在 + 仓别【无变动】  → 原样保留（无颜色）
│   └─ 烨辉表中【不存在】            → 原样保留（不删减）
│
└─ 原文件没有的钢卷
    ├─ 期货排程有订单 + 烨辉表有数据   → 新增 🟡 黄色（标注修改日期）
    └─ 非期货排程订单                  → 跳过（不引入无关数据）
```

> **只增加不删减**：原文件中已有的钢卷，不论烨辉表是否存在，永远不会被删除。

---

## 🎨 颜色标注说明

| 颜色 | 含义 | 触发条件 |
|------|------|----------|
| 🟠 **橙色** `#FFA500` | 仓别已更新 | 该钢卷的倉別在烨辉表中与原文件不一致 |
| 🟡 **黄色** `#FFFF00` | 新增钢卷 | 钢卷号不在原文件中，从烨辉表首次导入 |
| ⬜ **无色** | 原数据保留 | 仓别无变化，或烨辉表中不存在（保留） |
| 🟢 **绿色** `#00FF00` | 排程已匹配 | 期货排程中的订单在烨辉表或客户 Sheet 中有对应 |
| 🔴 **红色** `#FF0000` | 排程未匹配 | 期货排程中的订单在两处都无对应 |

---

## 📊 列字段映射

### 原文件（期货库存明细）客户 Sheet

| 列号 | 字段名 | 用途 |
|------|--------|------|
| Col 3 | 訂單編號 | 订单识别 |
| Col 4 | 鋼捲編號 | **唯一标识**，用于去重判断 |
| Col 12 | **倉別** | 读取当前仓别，与烨辉表对比 |
| Col 13 | **移撥日期** | 仓别更新时同步更新 |
| Col 14 | **入庫日期** | 仓别更新时同步更新 |
| Col 24 | **修改日期** | 更新/新增时标注运行日期 |

### 烨辉库存表

| 列号 | 字段名 | 用途 |
|------|--------|------|
| Col 1 | 订单编号 | 关联期货排程中的订单 |
| Col 8 | **鋼捲編號** | 钢卷唯一标识 |
| Col 29 | **入庫日期** | 更新时写入原文件 Col 14 |
| Col 32 | **倉別** | 与原文件 Col 12 对比，不同则触发更新 |
| Col 33 | **移撥日期** | 更新时写入原文件 Col 13 |

### 期货排程

| 列号 | 字段名 | 用途 |
|------|--------|------|
| Col 1 | 合同日期 | 新增钢卷时写入目标 Sheet |
| Col 2 | 客户名称 | 决定写入哪个客户 Sheet |
| Col 3 | 訂單號碼 | 关联烨辉表中的钢卷数据 |

---

## 🚀 使用方法（Windows 用户）

### 方式一：双击运行（推荐，有进度窗口）

1. 关闭所有打开的 Excel 文件
2. 双击 `run.bat`
3. 等待黑色窗口显示处理完成
4. 按任意键关闭窗口

### 方式二：静默运行（无黑窗口，完成后弹窗提示）

1. 关闭所有打开的 Excel 文件
2. 双击 `run_silent.vbs`
3. 后台自动处理
4. 完成后弹出消息框显示结果统计

### 方式三：命令行运行

```bash
# 基础运行
python cli.py

# 指定工作目录
python cli.py --base-dir "C:\Users\77188\Desktop\期货库存"

# 预览模式（只比对不保存）
python cli.py --dry-run

# 详细日志
python cli.py -v
```

### 方式四：AI 工具直接调用

```bash
# JSON 输出模式（适合 AI 解析）
python cli.py --json

# 预览 + JSON 输出
python cli.py --dry-run --json

# 指定目录 + JSON 输出
python cli.py --base-dir "C:\Users\77188\Desktop\期货库存" --json
```

**JSON 输出示例：**

```json
{
  "tool": "HSXFuturesInvMgr",
  "version": "5.0",
  "timestamp": "2026-05-10T10:30:00",
  "exit_code": 0,
  "status": "success",
  "data": {
    "preserved": 120,
    "updated": 5,
    "new": 8,
    "matched": 45,
    "unmatched": 2
  }
}
```

**退出码规范：**

| 退出码 | 含义 | 说明 |
|--------|------|------|
| 0 | 成功 | 处理完成，无错误 |
| 1 | 错误 | 文件未找到、Excel 被占用等 |
| 2 | 警告 | 处理完成，但存在未匹配订单 |

---

## 🔧 首次使用 / 环境检查

如果是新电脑或不确定环境是否配置正确，先运行：

```bash
python install.py
```

该脚本会自动：
- ✅ 检查 Python 版本（需 3.12+）
- ✅ 检查 `openpyxl` 是否安装（未安装则自动安装）
- ✅ 检查项目文件完整性
- ✅ 提示下一步操作

---

## 📁 文件结构

```
期货库存/
├── cli.py                      ← 主 CLI 入口（推荐）
├── main.py                     ← 兼容旧版入口
├── config.py                   ← 配置常量
├── utils.py                    ← 通用工具函数
├── models.py                   ← 数据模型
├── excel_handler.py            ← Excel 读写封装
├── processor.py                ← 核心业务逻辑
├── install.py                  ← 环境检查安装脚本
├── run.bat                     ← 双击运行（有窗口）
├── run_silent.vbs              ← 静默运行（无窗口）
├── 启动器.py                    ← Python 启动器
├── 使用说明.md                  ← 本文档
├── README.md                   ← 项目简介
├── requirements.txt            ← Python 依赖
└── results/
    ├── 期货库存明细.xlsx        ← 源文件 & 输出文件（直接覆盖更新）
    ├── 期货库存明细_备份.xlsx    ← 自动备份文件（覆盖模式）
    └── 烨辉库存表.xlsx          ← 烨辉库存数据源
```

---

## ⚠️ 注意事项

| 注意点 | 说明 |
|--------|------|
| 文件占用 | 运行前请关闭 Excel，否则保存失败 |
| 临时文件 | `~$*.xlsx` 自动跳过 |
| 备份文件 | 文件名含「备份」的自动跳过；程序自动创建「期货库存明细_备份.xlsx」 |
| 备份模式 | 覆盖模式，只保留一份最新备份，重复运行会覆盖旧备份 |
| Sheet名限制 | 最长 31 字符，特殊字符自动替换为 `_` |
| 仓别比较 | 精确字符串比较，注意全/半角、空格差异 |
| 钢卷号去重 | 以钢卷号为唯一 key，同一钢卷不会重复添加 |
| 首次运行 | 建议先运行 `install.py` 检查环境 |

---

## 🔄 版本历史

### v5.0 (2026-05-10) — TDD 重构版

- ✅ **代码重构**：将单体脚本拆分为 6 个独立模块 + CLI 接口
- ✅ **TDD 开发**：72 个单元测试覆盖所有核心逻辑
- ✅ **AI 友好**：新增 `--json` 结构化输出，标准退出码
- ✅ **预览模式**：新增 `--dry-run`，只比对不保存
- ✅ **Windows 优化**：`run.bat` + `run_silent.vbs` 双击运行
- ✅ **环境检查**：`install.py` 自动检测并安装依赖
- ✅ **消除硬编码**：路径和配置提取到 `config.py`

### v4.5 (2026-05-08) 及更早版本

详见 `README.md` 中的版本历史。

---

## 🛠 技术依赖

| 依赖 | 版本 |
|------|------|
| Python | 3.12+ |
| openpyxl | 3.1.5+ |
| pytest | 9.0+（可选，用于运行测试）|

---

*文档版本: v5.0 | 更新日期: 2026-05-10*
